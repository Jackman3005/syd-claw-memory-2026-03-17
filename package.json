{
  "name": "sydclaw-memory-talk",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "bun run build.ts",
    "preview": "cd dist && bunx serve -l 3000",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "reveal.js": "^6.0.0"
  }
}
